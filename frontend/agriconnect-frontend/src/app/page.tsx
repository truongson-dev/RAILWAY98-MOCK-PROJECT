import { LandingPage } from '@/components/pages/LandingPage';

// Root landing page — Server Component shell;
// interactive pieces are in the LandingPage Client Component.
export default function HomePage() {
  return <LandingPage />;
}
